<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gameandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameAndroid"
        tools:ignore="AllowBackup">
        <activity android:name=".Views.Options"
            android:screenOrientation="landscape">
        </activity>
        <activity android:name=".Views.ModeSettings" />
        <activity android:name=".Views.SoundSettings" />
        <activity
            android:name=".Views.Game"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Views.Home"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>